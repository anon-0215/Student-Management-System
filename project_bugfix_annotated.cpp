// ============================================================================
// 逐行注释教学版：校园学生食堂信息管理系统
// 说明：本文件基于 project.cpp 生成。功能性修改只有一处：
//       dishManageMenu 中 readInt("请选择：", 0, 3) 改为 readInt("请选择：", 0, 4)，
//       这样菜单第 4 项“修改菜品”才能被选择。其余新增内容均为注释。
// ============================================================================

// 【原第0001行】开始一段文件头注释，用来说明整个程序的主题和设计目标，不参与编译。
/*
 // 【原第0002行】这是文件头注释中的说明文字，帮助读者理解程序目的。
 * 校园学生食堂信息管理系统（题目 1 简化规范版）
 // 【原第0003行】这是文件头注释中的说明文字，帮助读者理解程序目的。
 * ------------------------------------------------
 // 【原第0004行】这是文件头注释中的说明文字，帮助读者理解程序目的。
 * 设计目标：
 // 【原第0005行】这是文件头注释中的说明文字，帮助读者理解程序目的。
 * 1. 使用“学生类链表”保存学生信息，体现链表插入、删除、查找、遍历。
 // 【原第0006行】这是文件头注释中的说明文字，帮助读者理解程序目的。
 * 2. 管理员作为超级用户，通过“全局函数”管理学生、菜品并查询订单。
 // 【原第0007行】这是文件头注释中的说明文字，帮助读者理解程序目的。
 * 3. 学生登录后，通过“学生对象自己的成员函数”完成订餐、取餐、查询。
 // 【原第0008行】这是文件头注释中的说明文字，帮助读者理解程序目的。
 * 4. 菜品使用结构体数组 vector 保存，代码难度适合大一学生阅读和汇报。
 // 【原第0009行】这是文件头注释中的说明文字，帮助读者理解程序目的。
 *
 // 【原第0010行】这是文件头注释中的说明文字，帮助读者理解程序目的。
 * 编码说明：本源码使用 UTF-8 编码保存。
 // 【原第0011行】这是文件头注释中的说明文字，帮助读者理解程序目的。
 */
 
// 【原第0013行】引入 C++ 标准库头文件，让程序能使用对应的库函数或类型。
#include <algorithm>
// 【原第0014行】引入 C++ 标准库头文件，让程序能使用对应的库函数或类型。
#include <cstdlib>
// 【原第0015行】引入 C++ 标准库头文件，让程序能使用对应的库函数或类型。
#include <iomanip>
// 【原第0016行】引入 C++ 标准库头文件，让程序能使用对应的库函数或类型。
#include <iostream>
// 【原第0017行】引入 C++ 标准库头文件，让程序能使用对应的库函数或类型。
#include <limits> 
// 【原第0018行】引入 C++ 标准库头文件，让程序能使用对应的库函数或类型。
#include <string>
// 【原第0019行】引入 C++ 标准库头文件，让程序能使用对应的库函数或类型。
#include <vector>

// 【原第0021行】使用 std 命名空间，后面可以直接写 cout、string、vector，而不用写 std::cout。
using namespace std;

// 【原第0023行】下面进入一个新的功能区域，分隔线只用于提高代码可读性。
// ========================= 常量与基础类型 =========================

// 【原第0025行】定义常量，程序运行过程中这个值不会被修改。
const string ADMIN_NAME = "admin";
// 【原第0026行】定义常量，程序运行过程中这个值不会被修改。
const string ADMIN_PASSWORD = "123456";
// 【原第0027行】定义常量，程序运行过程中这个值不会被修改。
const int LOW_STOCK_LINE = 5;       // 库存低于等于该值时提示库存偏低
// 【原第0028行】定义常量，程序运行过程中这个值不会被修改。
const int SCREEN_WIDTH = 72;        // 控制台分隔线宽度，仅用于美化显示

// 【原第0030行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
// 订单状态：题目 1 只需要区分“已预订”和“已取餐”即可。
// 【原第0031行】定义强类型枚举，用固定选项表示订单状态，比普通整数更安全。
enum class OrderStatus 
// 【原第0032行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
    // 【原第0033行】这是枚举中的一个可选状态值。
    Reserved,
    // 【原第0034行】这是枚举中的一个可选状态值。
    PickedUp
// 【原第0035行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
};

// 【原第0037行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
// 菜品信息：题目要求包含菜品名、价格、数量、口味、营养、过敏源、评分等。
// 【原第0038行】定义结构体类型，把一组相关字段打包成一个数据类型。
struct Dish 
// 【原第0039行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
    // 【原第0040行】id 是编号字段，菜品和学生查找都依赖编号或学号。
    int id;                 // 菜品编号，管理员和学生都通过编号选择菜品
    // 【原第0041行】name 保存姓名，用于界面显示和查询结果显示。
    string name;            // 菜品名称
    // 【原第0042行】category 表示菜品分类，例如早餐、午餐、晚餐、特色菜。
    string category;        // 分类：早餐、午餐、晚餐、特色菜等
    // 【原第0043行】price 表示菜品价格，用 double 保存小数。
    double price;           // 单价
    // 【原第0044行】stock 表示库存数量，订餐成功后会减少。
    int stock;              // 当前库存数量
    // 【原第0045行】flavor 表示口味说明，例如清淡、麻辣。
    string flavor;          // 口味说明
    // 【原第0046行】nutrition 表示营养成分简述。
    string nutrition;       // 营养成分简述
    // 【原第0047行】allergen 表示过敏源信息。
    string allergen;        // 过敏源信息
    // 【原第0048行】rating 表示菜品平均评分。
    double rating;          // 平均评分，本题 1 只展示，不做复杂评价统计
// 【原第0049行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
};

// 【原第0051行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
// 订单信息：每个学生对象内部保存自己的订单记录。
// 【原第0052行】定义结构体类型，把一组相关字段打包成一个数据类型。
struct Order 
// 【原第0053行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
    // 【原第0054行】orderId 是订单编号，用来区分不同订单。
    int orderId;            // 订单编号
    // 【原第0055行】dishId 记录订单对应的菜品编号。
    int dishId;             // 菜品编号
    // 【原第0056行】dishName 记录下单时的菜品名，即使菜品后来改名，订单仍能显示原名字。
    string dishName;        // 下单时的菜品名，便于后续查询
    // 【原第0057行】quantity 表示订购数量。
    int quantity;           // 数量
    // 【原第0058行】amount 表示订单总金额。
    double amount;          // 金额
    // 【原第0059行】mealTime 表示预约用餐时段。
    string mealTime;        // 用餐时段，如 早餐 7:00-8:00
    // 【原第0060行】mode 表示堂食、自提或外卖。
    string mode;            // 用餐方式：堂食 / 自提 / 外卖
    // 【原第0061行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
    OrderStatus status;     // 订单状态
    // 【原第0062行】pickupCode 是取餐码，用于取餐验证。
    string pickupCode;      // 取餐码，用于防止重复取餐
// 【原第0063行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
};

// 【原第0065行】下面进入一个新的功能区域，分隔线只用于提高代码可读性。
// ========================= 工具函数 =========================
// 【原第0066行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
// 工具函数不属于某个学生对象，因此设计为全局函数。



// 【原第0070行】定义打印分隔线的函数，参数 ch 有默认值 =，调用时可以不传参。
void printLine(char ch = '=') 
// 【原第0071行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
    // 【原第0072行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << string(SCREEN_WIDTH, ch) << '\n';
// 【原第0073行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0075行】定义暂停函数，让用户看清上一屏输出后再继续。
void pauseScreen() {
    // 【原第0076行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "\n按回车键继续...";
    // 【原第0077行】从键盘读取用户输入。
    cin.ignore(numeric_limits<streamsize>::max(), '\n');
// 【原第0078行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0080行】定义清理输入缓冲区的函数，防止错误输入影响下一次读取。
void clearInput() {
    // 【原第0081行】从键盘读取用户输入。
    cin.clear();
    // 【原第0082行】从键盘读取用户输入。
    cin.ignore(numeric_limits<streamsize>::max(), '\n');
// 【原第0083行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0085行】定义安全读取整数的函数：提示用户输入，并限制最小值和最大值。
int readInt(const string& prompt, int minValue, int maxValue) {
    // 【原第0086行】声明一个变量或对象，用来保存后续计算或管理的数据。
    int value;
    // 【原第0087行】while 循环：只要条件成立，就反复执行循环体。
    while (true) {
        // 【原第0088行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << prompt;
        // 【原第0089行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (cin >> value && value >= minValue && value <= maxValue) {
            // 【原第0090行】调用清理输入函数，保证下一次输入从干净状态开始。
            clearInput();
            // 【原第0091行】return 把结果返回给调用者，并结束当前函数。
            return value;
        // 【原第0092行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0093行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "输入无效，请输入 " << minValue << " 到 " << maxValue << " 之间的整数。\n";
        // 【原第0094行】调用清理输入函数，保证下一次输入从干净状态开始。
        clearInput();
    // 【原第0095行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0096行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0098行】定义安全读取小数的函数：用于价格、评分等带小数的数据。
double readDouble(const string& prompt, double minValue, double maxValue)
 // 【原第0099行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
 {
    // 【原第0100行】声明一个变量或对象，用来保存后续计算或管理的数据。
    double value;
    // 【原第0101行】while 循环：只要条件成立，就反复执行循环体。
    while (true) {
        // 【原第0102行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << prompt;
        // 【原第0103行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (cin >> value && value >= minValue && value <= maxValue) 
		// 【原第0104行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
		{
            // 【原第0105行】调用清理输入函数，保证下一次输入从干净状态开始。
            clearInput();
            // 【原第0106行】return 把结果返回给调用者，并结束当前函数。
            return value;
        // 【原第0107行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0108行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "输入无效，请输入 " << minValue << " 到 " << maxValue << " 之间的数字。\n";
        // 【原第0109行】调用清理输入函数，保证下一次输入从干净状态开始。
        clearInput();
    // 【原第0110行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0111行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0113行】定义读取一整行文本的函数，可选择是否允许空字符串。
string readText(const string& prompt, bool allowEmpty = false) 
// 【原第0114行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
    // 【原第0115行】声明一个变量或对象，用来保存后续计算或管理的数据。
    string text;
    // 【原第0116行】while 循环：只要条件成立，就反复执行循环体。
    while (true) {
        // 【原第0117行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << prompt;
        // 【原第0118行】getline 读取一整行文本，适合读取姓名、专业、菜名等字符串。
        getline(cin, text);
        // 【原第0119行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (allowEmpty || !text.empty())
		 // 【原第0120行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
		 {
            // 【原第0121行】return 把结果返回给调用者，并结束当前函数。
            return text;
        // 【原第0122行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0123行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "内容不能为空，请重新输入。\n";
    // 【原第0124行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0125行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0127行】定义订单状态转换函数，把枚举值转换成中文显示文字。
string orderStatusToString(OrderStatus status)
 // 【原第0128行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
 {
    // 【原第0129行】return 把结果返回给调用者，并结束当前函数。
    return status == OrderStatus::Reserved ? "已预订" : "已取餐";
// 【原第0130行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0132行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
// 根据菜品编号查找菜品，返回 vector 下标；找不到返回 -1。
// 【原第0133行】定义按菜品编号查找 vector 下标的函数，找不到就返回 -1。
int findDishIndexById(const vector<Dish>& dishes, int dishId)
 // 【原第0134行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
 {
    // 【原第0135行】for 循环：按指定规则遍历数据，这里常用于遍历数组、vector 或链表。
    for (size_t i = 0; i < dishes.size(); ++i)
	 // 【原第0136行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	 {
        // 【原第0137行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (dishes[i].id == dishId) 
		// 【原第0138行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
		{
            // 【原第0139行】return 把结果返回给调用者，并结束当前函数。
            return static_cast<int>(i);
        // 【原第0140行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
    // 【原第0141行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
    // 【原第0142行】return 把结果返回给调用者，并结束当前函数。
    return -1;
// 【原第0143行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0145行】定义自动生成新菜品编号的函数，避免手动输入重复编号。
int getNextDishId(const vector<Dish>& dishes)
 // 【原第0146行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
 {
    // 【原第0147行】声明一个变量或对象，用来保存后续计算或管理的数据。
    int maxId = 1000;
    // 【原第0148行】for 循环：按指定规则遍历数据，这里常用于遍历数组、vector 或链表。
    for (const Dish& dish : dishes) {
        // 【原第0149行】赋值语句，把右边的结果保存到左边的变量或成员中。
        maxId = max(maxId, dish.id);
    // 【原第0150行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
    // 【原第0151行】return 把结果返回给调用者，并结束当前函数。
    return maxId + 1;
// 【原第0152行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0154行】定义生成取餐码的函数，用学号尾号和订单号拼出简单验证码。
string makePickupCode(const string& studentId, int orderId) {
    // 【原第0155行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
    // 简单生成取餐码：学号后四位 + 订单号。
    // 【原第0156行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
    // 这不是安全加密，只是课程实验中的取餐验证模拟。
    // 【原第0157行】studentId 表示学号，是学生登录和查找学生的唯一标识。
    string tail = studentId;
    // 【原第0158行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (tail.size() > 4) {
        // 【原第0159行】size() 获取字符串或容器的元素个数。
        tail = tail.substr(tail.size() - 4);
    // 【原第0160行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
    // 【原第0161行】return 把结果返回给调用者，并结束当前函数。
    return tail + "-" + to_string(orderId);
// 【原第0162行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0164行】定义显示菜品表头的函数，只负责输出表格标题。
void showDishTableHeader() {
    // 【原第0165行】调用打印分隔线函数，让控制台界面更清楚。
    printLine('-');
    // 【原第0166行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << left
         // 【原第0167行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(8) << "编号"
         // 【原第0168行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(14) << "名称"
         // 【原第0169行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(10) << "分类"
         // 【原第0170行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(10) << "价格"
         // 【原第0171行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(8) << "库存"
         // 【原第0172行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(12) << "口味"
         // 【原第0173行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(8) << "评分" << '\n';
    // 【原第0174行】调用打印分隔线函数，让控制台界面更清楚。
    printLine('-');
// 【原第0175行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0177行】定义显示单个菜品简略信息的函数，用于列表展示。
void showDishBrief(const Dish& dish) {
    // 【原第0178行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << left
         // 【原第0179行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(8) << dish.id
         // 【原第0180行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(14) << dish.name
         // 【原第0181行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(10) << dish.category
         // 【原第0182行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(10) << fixed << setprecision(2) << dish.price
         // 【原第0183行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(8) << dish.stock
         // 【原第0184行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(12) << dish.flavor
         // 【原第0185行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(8) << fixed << setprecision(1) << dish.rating;
    // 【原第0186行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (dish.stock <= LOW_STOCK_LINE) {
        // 【原第0187行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "  库存偏低";
    // 【原第0188行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
    // 【原第0189行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << '\n';
// 【原第0190行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0192行】定义显示单个菜品详细信息的函数，用于查看详情。
void showDishDetail(const Dish& dish) 
// 【原第0193行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
    // 【原第0194行】调用打印分隔线函数，让控制台界面更清楚。
    printLine('-');
    // 【原第0195行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "菜品编号：" << dish.id << '\n';
    // 【原第0196行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "菜品名称：" << dish.name << '\n';
    // 【原第0197行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "菜品分类：" << dish.category << '\n';
    // 【原第0198行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "单价：" << fixed << setprecision(2) << dish.price << " 元\n";
    // 【原第0199行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "库存：" << dish.stock << " 份\n";
    // 【原第0200行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "口味：" << dish.flavor << '\n';
    // 【原第0201行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "营养成分：" << dish.nutrition << '\n';
    // 【原第0202行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "过敏源：" << dish.allergen << '\n';
    // 【原第0203行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "评分：" << fixed << setprecision(1) << dish.rating << " 星\n";
    // 【原第0204行】调用打印分隔线函数，让控制台界面更清楚。
    printLine('-');
// 【原第0205行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0207行】定义显示全部菜品的函数，遍历 vector 中所有菜品。
void showAllDishes(const vector<Dish>& dishes) 
// 【原第0208行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
    // 【原第0209行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (dishes.empty()) {
        // 【原第0210行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "当前没有菜品信息。\n";
        // 【原第0211行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
        return;
    // 【原第0212行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0214行】调用菜品显示相关函数，避免重复写输出代码。
    showDishTableHeader();
    // 【原第0215行】for 循环：按指定规则遍历数据，这里常用于遍历数组、vector 或链表。
    for (const Dish& dish : dishes) 
	// 【原第0216行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第0217行】调用菜品显示相关函数，避免重复写输出代码。
        showDishBrief(dish);
    // 【原第0218行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
    // 【原第0219行】调用打印分隔线函数，让控制台界面更清楚。
    printLine('-');
// 【原第0220行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0222行】下面进入一个新的功能区域，分隔线只用于提高代码可读性。
// ========================= 学生类 =========================
// 【原第0223行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
// 学生类既保存学生基本信息，也保存该学生自己的订单。
// 【原第0224行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
// next 指针用于把多个学生对象连接成“学生类链表”。

// 【原第0226行】定义学生类，学生对象既保存基本信息，也保存自己的订单。
class Student 
// 【原第0227行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
// 【原第0228行】private 表示下面的数据只能在类内部直接访问，外部不能随便改。
private:
    // 【原第0229行】studentId 表示学号，是学生登录和查找学生的唯一标识。
    string studentId;
    // 【原第0230行】password 保存该学生登录密码。
    string password;
    // 【原第0231行】name 保存姓名，用于界面显示和查询结果显示。
    string name;
    // 【原第0232行】gender 保存性别信息。
    string gender;
    // 【原第0233行】birthDate 保存出生日期。
    string birthDate;
    // 【原第0234行】grade 保存年级，例如大一、大二。
    string grade;
    // 【原第0235行】major 保存专业名称。
    string major;
    // 【原第0236行】phone 保存联系电话。
    string phone;
    // 【原第0237行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
    vector<Order> orders;

// 【原第0239行】public 表示下面的成员可以被类外部调用或访问。
public:
    // 【原第0240行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
    Student* next;

    // 【原第0242行】定义学生类构造函数，创建学生对象时一次性填入基本信息。
    Student(const string& id,
            // 【原第0243行】定义常量，程序运行过程中这个值不会被修改。
            const string& pwd,
            // 【原第0244行】定义常量，程序运行过程中这个值不会被修改。
            const string& stuName,
            // 【原第0245行】定义常量，程序运行过程中这个值不会被修改。
            const string& stuGender,
            // 【原第0246行】定义常量，程序运行过程中这个值不会被修改。
            const string& birthday,
            // 【原第0247行】定义常量，程序运行过程中这个值不会被修改。
            const string& stuGrade,
            // 【原第0248行】定义常量，程序运行过程中这个值不会被修改。
            const string& stuMajor,
            // 【原第0249行】定义常量，程序运行过程中这个值不会被修改。
            const string& stuPhone)
        // 【原第0250行】这是构造函数初始化列表的一部分，在对象创建时直接初始化数据成员。
        : studentId(id),
          // 【原第0251行】这是构造函数初始化列表的一部分，在对象创建时直接初始化数据成员。
          password(pwd),
          // 【原第0252行】这是构造函数初始化列表的一部分，在对象创建时直接初始化数据成员。
          name(stuName),
          // 【原第0253行】这是构造函数初始化列表的一部分，在对象创建时直接初始化数据成员。
          gender(stuGender),
          // 【原第0254行】这是构造函数初始化列表的一部分，在对象创建时直接初始化数据成员。
          birthDate(birthday),
          // 【原第0255行】这是构造函数初始化列表的一部分，在对象创建时直接初始化数据成员。
          grade(stuGrade),
          // 【原第0256行】这是构造函数初始化列表的一部分，在对象创建时直接初始化数据成员。
          major(stuMajor),
          // 【原第0257行】这是构造函数初始化列表的一部分，在对象创建时直接初始化数据成员。
          phone(stuPhone),
          // 【原第0258行】这是构造函数初始化列表的一部分，在对象创建时直接初始化数据成员。
          next(nullptr) {}

    // 【原第0260行】定义获取学号的成员函数，const 表示不会修改学生对象。
    string getId() const { return studentId; }
    // 【原第0261行】定义获取姓名的成员函数，供菜单和查询显示使用。
    string getName() const { return name; }
    // 【原第0262行】定义获取年级的成员函数。
    string getGrade() const { return grade; }
    // 【原第0263行】定义获取专业的成员函数。
    string getMajor() const { return major; }
    // 【原第0264行】定义获取订单列表的成员函数，返回 const 引用避免复制并禁止外部修改。
    const vector<Order>& getOrders() const { return orders; }

    // 【原第0266行】定义密码校验成员函数，用输入密码和对象内保存的密码比较。
    bool checkPassword(const string& inputPassword) const 
	// 【原第0267行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第0268行】return 把结果返回给调用者，并结束当前函数。
        return password == inputPassword;
    // 【原第0269行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0271行】定义学生简略信息输出函数，用于学生列表表格。
    void showBrief() const 
	// 【原第0272行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第0273行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << left
             // 【原第0274行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(14) << studentId
             // 【原第0275行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(10) << name
             // 【原第0276行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(8) << gender
             // 【原第0277行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(10) << grade
             // 【原第0278行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(18) << major
             // 【原第0279行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(14) << phone << '\n';
    // 【原第0280行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0282行】定义学生个人详细信息输出函数。
    void showProfile() const
	 // 【原第0283行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	 {
        // 【原第0284行】调用打印分隔线函数，让控制台界面更清楚。
        printLine('-');
        // 【原第0285行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "学号：" << studentId << '\n';
        // 【原第0286行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "姓名：" << name << '\n';
        // 【原第0287行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "性别：" << gender << '\n';
        // 【原第0288行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "出生日期：" << birthDate << '\n';
        // 【原第0289行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "年级：" << grade << '\n';
        // 【原第0290行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "专业：" << major << '\n';
        // 【原第0291行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "联系电话：" << phone << '\n';
        // 【原第0292行】调用打印分隔线函数，让控制台界面更清楚。
        printLine('-');
    // 【原第0293行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0295行】定义管理员修改该学生信息的成员函数，修改的是当前 this 对象的数据。
    void modifyByAdmin() {
        // 【原第0296行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
        // 管理员修改学生信息时调用该成员函数。
        // 【原第0297行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
        // 修改动作由管理员菜单触发，但具体数据仍属于这个学生对象本身。
        // 【原第0298行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "正在修改学生【" << name << "】的信息。直接回车表示保留原内容。\n";

        // 【原第0300行】name 保存姓名，用于界面显示和查询结果显示。
        string newName = readText("姓名（当前：" + name + "）：", true);
        // 【原第0301行】gender 保存性别信息。
        string newGender = readText("性别（当前：" + gender + "）：", true);
        // 【原第0302行】birthDate 保存出生日期。
        string newBirthDate = readText("出生日期（当前：" + birthDate + "）：", true);
        // 【原第0303行】grade 保存年级，例如大一、大二。
        string newGrade = readText("年级（当前：" + grade + "）：", true);
        // 【原第0304行】major 保存专业名称。
        string newMajor = readText("专业（当前：" + major + "）：", true);
        // 【原第0305行】phone 保存联系电话。
        string newPhone = readText("联系电话（当前：" + phone + "）：", true);

        // 【原第0307行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (!newName.empty()) name = newName;
        // 【原第0308行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (!newGender.empty()) gender = newGender;
        // 【原第0309行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (!newBirthDate.empty()) birthDate = newBirthDate;
        // 【原第0310行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (!newGrade.empty()) grade = newGrade;
        // 【原第0311行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (!newMajor.empty()) major = newMajor;
        // 【原第0312行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (!newPhone.empty()) phone = newPhone;

        // 【原第0314行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "学生信息修改完成。\n";
    // 【原第0315行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0317行】定义学生查看菜品的成员函数，体现学生对象发起查看操作。
    void viewDishes(const vector<Dish>& dishes) const
	 // 【原第0318行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	 {
        // 【原第0319行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
        // 成员函数：学生查看菜品。
        // 【原第0320行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
        // 虽然菜品列表是外部数据，但“查看”这个动作是当前学生对象发起的。
        // 【原第0321行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "\n【" << name << "】正在查看今日菜单\n";
        // 【原第0322行】调用显示全部菜品的函数。
        showAllDishes(dishes);

        // 【原第0324行】声明一个变量或对象，用来保存后续计算或管理的数据。
        int choice = readInt("是否查看某个菜品详情？1.是  0.否：", 0, 1);
        // 【原第0325行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (choice == 1) {
            // 【原第0326行】dishId 记录订单对应的菜品编号。
            int dishId = readInt("请输入菜品编号：", 1, 999999);
            // 【原第0327行】dishId 记录订单对应的菜品编号。
            int index = findDishIndexById(dishes, dishId);
            // 【原第0328行】if 条件判断：括号里的条件成立才执行后面的代码块。
            if (index == -1) {
                // 【原第0329行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                cout << "没有找到该菜品。\n";
            // 【原第0330行】这一行打开一个新的代码块，后面的语句都属于这个作用范围。
            } else {
                // 【原第0331行】调用菜品显示相关函数，避免重复写输出代码。
                showDishDetail(dishes[index]);
            // 【原第0332行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
            }
        // 【原第0333行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
    // 【原第0334行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0336行】定义学生订餐成员函数：选择菜品、生成订单、扣减库存。
    void placeOrder(vector<Dish>& dishes, int& nextOrderId)
	 // 【原第0337行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	 {
        // 【原第0338行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
        // 成员函数：当前学生对象为自己订餐。
        // 【原第0339行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
        // 订单会被保存到 this->orders 中，这正是成员函数适合做的事情。
        // 【原第0340行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (dishes.empty()) {
            // 【原第0341行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
            cout << "当前没有可订购的菜品。\n";
            // 【原第0342行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
            return;
        // 【原第0343行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }

        // 【原第0345行】调用显示全部菜品的函数。
        showAllDishes(dishes);
        // 【原第0346行】dishId 记录订单对应的菜品编号。
        int dishId = readInt("请输入要订购的菜品编号：", 1, 999999);
        // 【原第0347行】dishId 记录订单对应的菜品编号。
        int index = findDishIndexById(dishes, dishId);
        // 【原第0348行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (index == -1) {
            // 【原第0349行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
            cout << "菜品编号不存在，订餐失败。\n";
            // 【原第0350行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
            return;
        // 【原第0351行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }

        // 【原第0353行】赋值语句，把右边的结果保存到左边的变量或成员中。
        Dish& selectedDish = dishes[index];
        // 【原第0354行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (selectedDish.stock <= 0) {
            // 【原第0355行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
            cout << "该菜品已经售罄，订餐失败。\n";
            // 【原第0356行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
            return;
        // 【原第0357行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }

        // 【原第0359行】stock 表示库存数量，订餐成功后会减少。
        int quantity = readInt("请输入订购数量：", 1, selectedDish.stock);

        // 【原第0361行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "\n请选择用餐时段：\n";
        // 【原第0362行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "1. 早餐 7:00-8:00\n";
        // 【原第0363行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "2. 午餐 11:30-12:30\n";
        // 【原第0364行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "3. 晚餐 17:30-18:30\n";
        // 【原第0365行】声明一个变量或对象，用来保存后续计算或管理的数据。
        int timeChoice = readInt("请选择：", 1, 3);

        // 【原第0367行】mealTime 表示预约用餐时段。
        string mealTime;
        // 【原第0368行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (timeChoice == 1) mealTime = "早餐 7:00-8:00";
        // 【原第0369行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (timeChoice == 2) mealTime = "午餐 11:30-12:30";
        // 【原第0370行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (timeChoice == 3) mealTime = "晚餐 17:30-18:30";

        // 【原第0372行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "\n请选择用餐方式：\n";
        // 【原第0373行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "1. 堂食\n";
        // 【原第0374行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "2. 自提\n";
        // 【原第0375行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "3. 外卖\n";
        // 【原第0376行】mode 表示堂食、自提或外卖。
        int modeChoice = readInt("请选择：", 1, 3);

        // 【原第0378行】mode 表示堂食、自提或外卖。
        string mode;
        // 【原第0379行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (modeChoice == 1) mode = "堂食";
        // 【原第0380行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (modeChoice == 2) mode = "自提";
        // 【原第0381行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (modeChoice == 3) mode = "外卖";

        // 【原第0383行】声明一个变量或对象，用来保存后续计算或管理的数据。
        Order newOrder;
        // 【原第0384行】赋值语句，把右边的结果保存到左边的变量或成员中。
        newOrder.orderId = nextOrderId++;
        // 【原第0385行】赋值语句，把右边的结果保存到左边的变量或成员中。
        newOrder.dishId = selectedDish.id;
        // 【原第0386行】赋值语句，把右边的结果保存到左边的变量或成员中。
        newOrder.dishName = selectedDish.name;
        // 【原第0387行】赋值语句，把右边的结果保存到左边的变量或成员中。
        newOrder.quantity = quantity;
        // 【原第0388行】赋值语句，把右边的结果保存到左边的变量或成员中。
        newOrder.amount = selectedDish.price * quantity;
        // 【原第0389行】赋值语句，把右边的结果保存到左边的变量或成员中。
        newOrder.mealTime = mealTime;
        // 【原第0390行】赋值语句，把右边的结果保存到左边的变量或成员中。
        newOrder.mode = mode;
        // 【原第0391行】赋值语句，把右边的结果保存到左边的变量或成员中。
        newOrder.status = OrderStatus::Reserved;
        // 【原第0392行】赋值语句，把右边的结果保存到左边的变量或成员中。
        newOrder.pickupCode = makePickupCode(studentId, newOrder.orderId);

        // 【原第0394行】向 vector 的末尾加入一个新元素。
        orders.push_back(newOrder);
        // 【原第0395行】赋值语句，把右边的结果保存到左边的变量或成员中。
        selectedDish.stock -= quantity;

        // 【原第0397行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "\n订餐成功！\n";
        // 【原第0398行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "订单编号：" << newOrder.orderId << '\n';
        // 【原第0399行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "菜品：" << newOrder.dishName << " × " << newOrder.quantity << '\n';
        // 【原第0400行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "金额：" << fixed << setprecision(2) << newOrder.amount << " 元\n";
        // 【原第0401行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "取餐码：" << newOrder.pickupCode << '\n';
        // 【原第0402行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "请妥善保存取餐码，取餐时需要验证。\n";
    // 【原第0403行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0405行】定义学生取餐成员函数：验证订单号和取餐码，防止重复取餐。
    void pickupOrder() {
        // 【原第0406行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
        // 成员函数：当前学生对象领取自己的订单。
        // 【原第0407行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
        // 取餐成功后，订单状态从“已预订”变为“已取餐”，防止重复领取。
        // 【原第0408行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (orders.empty()) {
            // 【原第0409行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
            cout << "你目前没有订单。\n";
            // 【原第0410行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
            return;
        // 【原第0411行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }

        // 【原第0413行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
        showOrders();
        // 【原第0414行】orderId 是订单编号，用来区分不同订单。
        int orderId = readInt("请输入要取餐的订单编号：", 1, 999999);
        // 【原第0415行】声明一个变量或对象，用来保存后续计算或管理的数据。
        string code = readText("请输入取餐码：");

        // 【原第0417行】for 循环：按指定规则遍历数据，这里常用于遍历数组、vector 或链表。
        for (Order& order : orders) {
            // 【原第0418行】if 条件判断：括号里的条件成立才执行后面的代码块。
            if (order.orderId == orderId) {
                // 【原第0419行】if 条件判断：括号里的条件成立才执行后面的代码块。
                if (order.status == OrderStatus::PickedUp) {
                    // 【原第0420行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                    cout << "该订单已经取过餐，不能重复领取。\n";
                    // 【原第0421行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
                    return;
                // 【原第0422行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
                }
                // 【原第0423行】if 条件判断：括号里的条件成立才执行后面的代码块。
                if (order.pickupCode != code) {
                    // 【原第0424行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                    cout << "取餐码错误，验证失败。\n";
                    // 【原第0425行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
                    return;
                // 【原第0426行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
                }

                // 【原第0428行】赋值语句，把右边的结果保存到左边的变量或成员中。
                order.status = OrderStatus::PickedUp;
                // 【原第0429行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                cout << "取餐成功，订单状态已更新为【已取餐】。\n";
                // 【原第0430行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
                return;
            // 【原第0431行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
            }
        // 【原第0432行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }

        // 【原第0434行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "没有找到该订单。\n";
    // 【原第0435行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0437行】定义学生查看自己订单的成员函数，只遍历当前学生对象的 orders。
    void showOrders() const {
        // 【原第0438行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
        // 成员函数：只显示当前学生自己的订单。
        // 【原第0439行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (orders.empty()) {
            // 【原第0440行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
            cout << "当前没有订单记录。\n";
            // 【原第0441行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
            return;
        // 【原第0442行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }

        // 【原第0444行】调用打印分隔线函数，让控制台界面更清楚。
        printLine('-');
        // 【原第0445行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << left
             // 【原第0446行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(10) << "订单号"
             // 【原第0447行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(14) << "菜品"
             // 【原第0448行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(8) << "数量"
             // 【原第0449行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(10) << "金额"
             // 【原第0450行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(18) << "时段"
             // 【原第0451行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(8) << "方式"
             // 【原第0452行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(10) << "状态" << '\n';
        // 【原第0453行】调用打印分隔线函数，让控制台界面更清楚。
        printLine('-');

        // 【原第0455行】for 循环：按指定规则遍历数据，这里常用于遍历数组、vector 或链表。
        for (const Order& order : orders) {
            // 【原第0456行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
            cout << left
                 // 【原第0457行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(10) << order.orderId
                 // 【原第0458行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(14) << order.dishName
                 // 【原第0459行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(8) << order.quantity
                 // 【原第0460行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(10) << fixed << setprecision(2) << order.amount
                 // 【原第0461行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(18) << order.mealTime
                 // 【原第0462行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(8) << order.mode
                 // 【原第0463行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(10) << orderStatusToString(order.status) << '\n';
        // 【原第0464行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0465行】调用打印分隔线函数，让控制台界面更清楚。
        printLine('-');
    // 【原第0466行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0467行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
};

// 【原第0469行】下面进入一个新的功能区域，分隔线只用于提高代码可读性。
// ========================= 学生链表类 =========================
// 【原第0470行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
// StudentList 负责管理链表本身：头指针、插入、删除、查找、遍历。
// 【原第0471行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
// 这样比把所有链表代码写在 main 里更清楚，也方便汇报。

// 【原第0473行】定义学生类，学生对象既保存基本信息，也保存自己的订单。
class StudentList 
// 【原第0474行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
// 【原第0475行】private 表示下面的数据只能在类内部直接访问，外部不能随便改。
private:
    // 【原第0476行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
    Student* head;

// 【原第0478行】public 表示下面的成员可以被类外部调用或访问。
public:
    // 【原第0479行】定义 StudentList 构造函数，初始化空链表。
    StudentList() : head(nullptr) {}

    // 【原第0481行】定义析构函数，对象销毁时自动释放链表中的动态内存。
    ~StudentList() 
	// 【原第0482行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第0483行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
        clear();
    // 【原第0484行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0486行】定义获取链表头指针的函数，方便管理员遍历所有学生。
    Student* getHead() const 
	// 【原第0487行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第0488行】return 把结果返回给调用者，并结束当前函数。
        return head;
    // 【原第0489行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0491行】定义判断链表是否为空的函数。
    bool isEmpty() const 
	// 【原第0492行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第0493行】return 把结果返回给调用者，并结束当前函数。
        return head == nullptr;
    // 【原第0494行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0496行】定义清空链表的函数，逐个 delete 每个学生结点。
    void clear() 
	// 【原第0497行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第0498行】赋值语句，把右边的结果保存到左边的变量或成员中。
        Student* current = head;
        // 【原第0499行】while 循环：只要条件成立，就反复执行循环体。
        while (current != nullptr) 
		// 【原第0500行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
		{
            // 【原第0501行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
            Student* nextNode = current->next;
            // 【原第0502行】delete 释放 new 创建的对象，防止内存泄漏。
            delete current;
            // 【原第0503行】赋值语句，把右边的结果保存到左边的变量或成员中。
            current = nextNode;
        // 【原第0504行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0505行】nullptr 表示空指针，即目前没有指向任何对象。
        head = nullptr;
    // 【原第0506行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0508行】定义按学号查找学生结点的函数，返回学生指针或 nullptr。
    Student* findById(const string& studentId) const 
	// 【原第0509行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第0510行】赋值语句，把右边的结果保存到左边的变量或成员中。
        Student* current = head;
        // 【原第0511行】while 循环：只要条件成立，就反复执行循环体。
        while (current != nullptr) 
		// 【原第0512行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
		{
            // 【原第0513行】if 条件判断：括号里的条件成立才执行后面的代码块。
            if (current->getId() == studentId) 
			// 【原第0514行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
			{
                // 【原第0515行】return 把结果返回给调用者，并结束当前函数。
                return current;
            // 【原第0516行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
            }
            // 【原第0517行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
            current = current->next;
        // 【原第0518行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0519行】return 把结果返回给调用者，并结束当前函数。
        return nullptr;
    // 【原第0520行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0522行】定义向链表添加学生的函数，使用头插法并检查学号重复。
    bool addStudent(Student* newStudent) 
	// 【原第0523行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第0524行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (newStudent == nullptr) 
		// 【原第0525行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
		{
            // 【原第0526行】return 把结果返回给调用者，并结束当前函数。
            return false;
        // 【原第0527行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0528行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (findById(newStudent->getId()) != nullptr) 
		// 【原第0529行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
		{
            // 【原第0530行】return 把结果返回给调用者，并结束当前函数。
            return false;
        // 【原第0531行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }

        // 【原第0533行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
        // 头插法：代码短，效率高，适合链表基础训练。
        // 【原第0534行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
        newStudent->next = head;
        // 【原第0535行】修改链表头指针，这是链表插入、删除或清空时的关键操作。
        head = newStudent;
        // 【原第0536行】return 把结果返回给调用者，并结束当前函数。
        return true;
    // 【原第0537行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0539行】定义按学号删除学生结点的函数，需要处理删除头结点和普通结点两种情况。
    bool removeById(const string& studentId) {
        // 【原第0540行】赋值语句，把右边的结果保存到左边的变量或成员中。
        Student* current = head;
        // 【原第0541行】nullptr 表示空指针，即目前没有指向任何对象。
        Student* previous = nullptr;

        // 【原第0543行】while 循环：只要条件成立，就反复执行循环体。
        while (current != nullptr) {
            // 【原第0544行】if 条件判断：括号里的条件成立才执行后面的代码块。
            if (current->getId() == studentId) {
                // 【原第0545行】if 条件判断：括号里的条件成立才执行后面的代码块。
                if (previous == nullptr) {
                    // 【原第0546行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
                    head = current->next;
                // 【原第0547行】这一行打开一个新的代码块，后面的语句都属于这个作用范围。
                } else {
                    // 【原第0548行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
                    previous->next = current->next;
                // 【原第0549行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
                }
                // 【原第0550行】delete 释放 new 创建的对象，防止内存泄漏。
                delete current;
                // 【原第0551行】return 把结果返回给调用者，并结束当前函数。
                return true;
            // 【原第0552行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
            }
            // 【原第0553行】移动链表指针，继续访问下一个学生结点。
            previous = current;
            // 【原第0554行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
            current = current->next;
        // 【原第0555行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0556行】return 把结果返回给调用者，并结束当前函数。
        return false;
    // 【原第0557行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0559行】定义遍历并显示所有学生的函数。
    void showAllStudents() const {
        // 【原第0560行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (isEmpty()) {
            // 【原第0561行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
            cout << "当前没有学生信息。\n";
            // 【原第0562行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
            return;
        // 【原第0563行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }

        // 【原第0565行】调用打印分隔线函数，让控制台界面更清楚。
        printLine('-');
        // 【原第0566行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << left
             // 【原第0567行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(14) << "学号"
             // 【原第0568行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(10) << "姓名"
             // 【原第0569行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(8) << "性别"
             // 【原第0570行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(10) << "年级"
             // 【原第0571行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(18) << "专业"
             // 【原第0572行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
             << setw(14) << "联系电话" << '\n';
        // 【原第0573行】调用打印分隔线函数，让控制台界面更清楚。
        printLine('-');

        // 【原第0575行】赋值语句，把右边的结果保存到左边的变量或成员中。
        Student* current = head;
        // 【原第0576行】while 循环：只要条件成立，就反复执行循环体。
        while (current != nullptr) {
            // 【原第0577行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
            current->showBrief();
            // 【原第0578行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
            current = current->next;
        // 【原第0579行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0580行】调用打印分隔线函数，让控制台界面更清楚。
        printLine('-');
    // 【原第0581行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0583行】定义统计学生人数的函数，通过遍历链表计数。
    int countStudents() const {
        // 【原第0584行】声明一个变量或对象，用来保存后续计算或管理的数据。
        int count = 0;
        // 【原第0585行】赋值语句，把右边的结果保存到左边的变量或成员中。
        Student* current = head;
        // 【原第0586行】while 循环：只要条件成立，就反复执行循环体。
        while (current != nullptr) {
            // 【原第0587行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
            ++count;
            // 【原第0588行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
            current = current->next;
        // 【原第0589行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0590行】return 把结果返回给调用者，并结束当前函数。
        return count;
    // 【原第0591行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0592行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
};

// 【原第0594行】下面进入一个新的功能区域，分隔线只用于提高代码可读性。
// ========================= 初始化数据 =========================

// 【原第0596行】定义初始化学生数据的函数，用几条测试数据方便演示。
void initStudents(StudentList& students) {
    // 【原第0597行】使用 new 在堆区动态创建一个 Student 对象，返回对象指针。
    students.addStudent(new Student("20260001", "111111", "李明", "男", "2007-03-12", "大一", "软件工程", "13800000001"));
    // 【原第0598行】使用 new 在堆区动态创建一个 Student 对象，返回对象指针。
    students.addStudent(new Student("20260002", "222222", "王雨", "女", "2007-07-05", "大一", "计算机科学", "13800000002"));
    // 【原第0599行】使用 new 在堆区动态创建一个 Student 对象，返回对象指针。
    students.addStudent(new Student("20260003", "333333", "张辰", "男", "2006-11-20", "大二", "网络工程", "13800000003"));
// 【原第0600行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0602行】定义初始化菜品数据的函数，用 push_back 加入测试菜品。
void initDishes(vector<Dish>& dishes) {
    // 【原第0603行】向 vector 的末尾加入一个新元素。
    dishes.push_back({1001, "鸡蛋灌饼", "早餐", 6.50, 30, "咸香", "蛋白质、碳水", "鸡蛋、面粉", 4.6});
    // 【原第0604行】向 vector 的末尾加入一个新元素。
    dishes.push_back({1002, "小米粥", "早餐", 3.00, 40, "清淡", "碳水、膳食纤维", "无明显过敏源", 4.4});
    // 【原第0605行】向 vector 的末尾加入一个新元素。
    dishes.push_back({1003, "红烧鸡腿", "午餐", 12.00, 25, "咸鲜", "蛋白质、脂肪", "大豆", 4.7});
    // 【原第0606行】向 vector 的末尾加入一个新元素。
    dishes.push_back({1004, "番茄炒蛋", "午餐", 8.00, 20, "酸甜", "维生素、蛋白质", "鸡蛋", 4.5});
    // 【原第0607行】向 vector 的末尾加入一个新元素。
    dishes.push_back({1005, "麻辣香锅", "晚餐", 18.00, 15, "麻辣", "蛋白质、脂肪", "花生、芝麻", 4.8});
    // 【原第0608行】向 vector 的末尾加入一个新元素。
    dishes.push_back({1006, "素炒西兰花", "特色菜", 7.00, 18, "清爽", "维生素、膳食纤维", "无明显过敏源", 4.3});
// 【原第0609行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0611行】下面进入一个新的功能区域，分隔线只用于提高代码可读性。
// ========================= 管理员全局函数：学生管理 =========================
// 【原第0612行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
// 这些函数没有 this 指针，不属于某一个学生对象。
// 【原第0613行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
// 它们代表管理员从系统整体角度进行管理，符合题目对“全局函数”的要求。

// 【原第0615行】定义从键盘读取学生信息并创建 Student 对象的全局函数。
Student* createStudentFromInput() {
    // 【原第0616行】声明一个变量或对象，用来保存后续计算或管理的数据。
    string id = readText("请输入学号：");
    // 【原第0617行】password 保存该学生登录密码。
    string password = readText("请输入初始密码：");
    // 【原第0618行】name 保存姓名，用于界面显示和查询结果显示。
    string name = readText("请输入姓名：");
    // 【原第0619行】gender 保存性别信息。
    string gender = readText("请输入性别：");
    // 【原第0620行】birthDate 保存出生日期。
    string birthDate = readText("请输入出生日期：");
    // 【原第0621行】grade 保存年级，例如大一、大二。
    string grade = readText("请输入年级：");
    // 【原第0622行】major 保存专业名称。
    string major = readText("请输入专业：");
    // 【原第0623行】phone 保存联系电话。
    string phone = readText("请输入联系电话：");

    // 【原第0625行】return 把结果返回给调用者，并结束当前函数。
    return new Student(id, password, name, gender, birthDate, grade, major, phone);
// 【原第0626行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0628行】定义管理员增加学生的全局函数。
void adminAddStudent(StudentList& students) {
    // 【原第0629行】赋值语句，把右边的结果保存到左边的变量或成员中。
    Student* newStudent = createStudentFromInput();
    // 【原第0630行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (students.addStudent(newStudent)) {
        // 【原第0631行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "学生添加成功。\n";
    // 【原第0632行】这一行打开一个新的代码块，后面的语句都属于这个作用范围。
    } else {
        // 【原第0633行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "添加失败：学号可能已经存在。\n";
        // 【原第0634行】delete 释放 new 创建的对象，防止内存泄漏。
        delete newStudent;
    // 【原第0635行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0636行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0638行】定义管理员删除学生的全局函数。
void adminDeleteStudent(StudentList& students) {
    // 【原第0639行】声明一个变量或对象，用来保存后续计算或管理的数据。
    string id = readText("请输入要删除的学生学号：");
    // 【原第0640行】赋值语句，把右边的结果保存到左边的变量或成员中。
    Student* student = students.findById(id);
    // 【原第0641行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (student == nullptr) {
        // 【原第0642行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "没有找到该学生。\n";
        // 【原第0643行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
        return;
    // 【原第0644行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0646行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "即将删除学生：" << student->getName() << "（" << student->getId() << "）\n";
    // 【原第0647行】声明一个变量或对象，用来保存后续计算或管理的数据。
    int confirm = readInt("确认删除？1.确认  0.取消：", 0, 1);
    // 【原第0648行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (confirm == 1 && students.removeById(id)) {
        // 【原第0649行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "删除成功。\n";
    // 【原第0650行】这一行打开一个新的代码块，后面的语句都属于这个作用范围。
    } else {
        // 【原第0651行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "已取消删除。\n";
    // 【原第0652行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0653行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0655行】定义管理员查询某个学生和该学生订单的全局函数。
void adminSearchStudent(const StudentList& students) {
    // 【原第0656行】声明一个变量或对象，用来保存后续计算或管理的数据。
    string id = readText("请输入要查询的学生学号：");
    // 【原第0657行】赋值语句，把右边的结果保存到左边的变量或成员中。
    Student* student = students.findById(id);
    // 【原第0658行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (student == nullptr) {
        // 【原第0659行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "没有找到该学生。\n";
        // 【原第0660行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
        return;
    // 【原第0661行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0663行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
    student->showProfile();
    // 【原第0664行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "该学生订单：\n";
    // 【原第0665行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
    student->showOrders();
// 【原第0666行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0668行】定义管理员修改学生信息的全局函数。
void adminModifyStudent(StudentList& students) {
    // 【原第0669行】声明一个变量或对象，用来保存后续计算或管理的数据。
    string id = readText("请输入要修改的学生学号：");
    // 【原第0670行】赋值语句，把右边的结果保存到左边的变量或成员中。
    Student* student = students.findById(id);
    // 【原第0671行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (student == nullptr) {
        // 【原第0672行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "没有找到该学生。\n";
        // 【原第0673行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
        return;
    // 【原第0674行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0676行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
    student->modifyByAdmin();
// 【原第0677行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0679行】定义管理员的学生数据管理子菜单函数。
void studentManageMenu(StudentList& students) {
    // 【原第0680行】while 循环：只要条件成立，就反复执行循环体。
    while (true) {
        // 【原第0681行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第0682行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "管理员菜单 > 学生数据管理\n";
        // 【原第0683行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第0684行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "1. 查看全部学生\n";
        // 【原第0685行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "2. 增加学生\n";
        // 【原第0686行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "3. 删除学生\n";
        // 【原第0687行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "4. 修改学生信息\n";
        // 【原第0688行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "5. 查询学生及其订单\n";
        // 【原第0689行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "0. 返回上一级\n";

        // 【原第0691行】声明一个变量或对象，用来保存后续计算或管理的数据。
        int choice = readInt("请选择：", 0, 5);
        // 【原第0692行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (choice == 0) return;

        // 【原第0694行】switch 多分支选择，根据 choice 的值进入不同 case。
        switch (choice) {
            // 【原第0695行】case 表示 switch 中的一个菜单选项分支。
            case 1:
                // 【原第0696行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
                students.showAllStudents();
                // 【原第0697行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                cout << "学生总人数：" << students.countStudents() << '\n';
                // 【原第0698行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第0699行】case 表示 switch 中的一个菜单选项分支。
            case 2:
                // 【原第0700行】调用管理员相关全局函数，处理系统整体层面的管理功能。
                adminAddStudent(students);
                // 【原第0701行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第0702行】case 表示 switch 中的一个菜单选项分支。
            case 3:
                // 【原第0703行】调用管理员相关全局函数，处理系统整体层面的管理功能。
                adminDeleteStudent(students);
                // 【原第0704行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第0705行】case 表示 switch 中的一个菜单选项分支。
            case 4:
                // 【原第0706行】调用管理员相关全局函数，处理系统整体层面的管理功能。
                adminModifyStudent(students);
                // 【原第0707行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第0708行】case 表示 switch 中的一个菜单选项分支。
            case 5:
                // 【原第0709行】调用管理员相关全局函数，处理系统整体层面的管理功能。
                adminSearchStudent(students);
                // 【原第0710行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
        // 【原第0711行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0712行】调用暂停函数，等待用户按回车后再继续显示菜单。
        pauseScreen();
    // 【原第0713行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0714行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0716行】下面进入一个新的功能区域，分隔线只用于提高代码可读性。
// ========================= 管理员全局函数：菜品管理 =========================

// 【原第0718行】定义管理员增加菜品的全局函数。
void adminAddDish(vector<Dish>& dishes) {
    // 【原第0719行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
    Dish dish;
    // 【原第0720行】赋值语句，把右边的结果保存到左边的变量或成员中。
    dish.id = getNextDishId(dishes);
    // 【原第0721行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "新菜品编号自动生成：" << dish.id << '\n';

    // 【原第0723行】调用封装好的输入函数，统一处理提示、范围检查和错误输入。
    dish.name = readText("请输入菜品名称：");
    // 【原第0724行】调用封装好的输入函数，统一处理提示、范围检查和错误输入。
    dish.category = readText("请输入菜品分类：");
    // 【原第0725行】调用封装好的输入函数，统一处理提示、范围检查和错误输入。
    dish.price = readDouble("请输入价格：", 0.1, 9999.0);
    // 【原第0726行】调用封装好的输入函数，统一处理提示、范围检查和错误输入。
    dish.stock = readInt("请输入库存数量：", 0, 99999);
    // 【原第0727行】调用封装好的输入函数，统一处理提示、范围检查和错误输入。
    dish.flavor = readText("请输入口味：");
    // 【原第0728行】调用封装好的输入函数，统一处理提示、范围检查和错误输入。
    dish.nutrition = readText("请输入营养成分：");
    // 【原第0729行】调用封装好的输入函数，统一处理提示、范围检查和错误输入。
    dish.allergen = readText("请输入过敏源信息：");
    // 【原第0730行】调用封装好的输入函数，统一处理提示、范围检查和错误输入。
    dish.rating = readDouble("请输入评分（1-5）：", 1.0, 5.0);

    // 【原第0732行】向 vector 的末尾加入一个新元素。
    dishes.push_back(dish);
    // 【原第0733行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "菜品添加成功。\n";
// 【原第0734行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0736行】定义管理员删除菜品的全局函数。
void adminDeleteDish(vector<Dish>& dishes) {
    // 【原第0737行】调用显示全部菜品的函数。
    showAllDishes(dishes);
    // 【原第0738行】dishId 记录订单对应的菜品编号。
    int dishId = readInt("请输入要删除的菜品编号：", 1, 999999);
    // 【原第0739行】dishId 记录订单对应的菜品编号。
    int index = findDishIndexById(dishes, dishId);
    // 【原第0740行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (index == -1) {
        // 【原第0741行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "没有找到该菜品。\n";
        // 【原第0742行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
        return;
    // 【原第0743行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0745行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "即将删除菜品：" << dishes[index].name << '\n';
    // 【原第0746行】声明一个变量或对象，用来保存后续计算或管理的数据。
    int confirm = readInt("确认删除？1.确认  0.取消：", 0, 1);
    // 【原第0747行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (confirm == 1) {
        // 【原第0748行】从 vector 中删除指定位置的元素。
        dishes.erase(dishes.begin() + index);
        // 【原第0749行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "删除成功。\n";
    // 【原第0750行】这一行打开一个新的代码块，后面的语句都属于这个作用范围。
    } else {
        // 【原第0751行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "已取消删除。\n";
    // 【原第0752行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0753行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0755行】定义管理员修改菜品信息的全局函数。
void adminModifyDish(vector<Dish>& dishes) {
    // 【原第0756行】调用显示全部菜品的函数。
    showAllDishes(dishes);
    // 【原第0757行】dishId 记录订单对应的菜品编号。
    int dishId = readInt("请输入要修改的菜品编号：", 1, 999999);
    // 【原第0758行】dishId 记录订单对应的菜品编号。
    int index = findDishIndexById(dishes, dishId);
    // 【原第0759行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (index == -1) {
        // 【原第0760行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "没有找到该菜品。\n";
        // 【原第0761行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
        return;
    // 【原第0762行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0764行】赋值语句，把右边的结果保存到左边的变量或成员中。
    Dish& dish = dishes[index];
    // 【原第0765行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "正在修改菜品【" << dish.name << "】。直接回车表示保留文字信息。\n";

    // 【原第0767行】name 保存姓名，用于界面显示和查询结果显示。
    string newName = readText("菜品名称（当前：" + dish.name + "）：", true);
    // 【原第0768行】category 表示菜品分类，例如早餐、午餐、晚餐、特色菜。
    string newCategory = readText("分类（当前：" + dish.category + "）：", true);
    // 【原第0769行】flavor 表示口味说明，例如清淡、麻辣。
    string newFlavor = readText("口味（当前：" + dish.flavor + "）：", true);
    // 【原第0770行】nutrition 表示营养成分简述。
    string newNutrition = readText("营养成分（当前：" + dish.nutrition + "）：", true);
    // 【原第0771行】allergen 表示过敏源信息。
    string newAllergen = readText("过敏源（当前：" + dish.allergen + "）：", true);

    // 【原第0773行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (!newName.empty()) dish.name = newName;
    // 【原第0774行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (!newCategory.empty()) dish.category = newCategory;
    // 【原第0775行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (!newFlavor.empty()) dish.flavor = newFlavor;
    // 【原第0776行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (!newNutrition.empty()) dish.nutrition = newNutrition;
    // 【原第0777行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (!newAllergen.empty()) dish.allergen = newAllergen;

    // 【原第0779行】声明一个变量或对象，用来保存后续计算或管理的数据。
    int changeNumber = readInt("是否修改价格、库存、评分？1.是  0.否：", 0, 1);
    // 【原第0780行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (changeNumber == 1) {
        // 【原第0781行】调用封装好的输入函数，统一处理提示、范围检查和错误输入。
        dish.price = readDouble("请输入新价格：", 0.1, 9999.0);
        // 【原第0782行】调用封装好的输入函数，统一处理提示、范围检查和错误输入。
        dish.stock = readInt("请输入新库存：", 0, 99999);
        // 【原第0783行】调用封装好的输入函数，统一处理提示、范围检查和错误输入。
        dish.rating = readDouble("请输入新评分（1-5）：", 1.0, 5.0);
    // 【原第0784行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0786行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "菜品信息修改完成。\n";
// 【原第0787行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0789行】定义管理员的菜品管理子菜单函数。
void dishManageMenu(vector<Dish>& dishes) {
    // 【原第0790行】while 循环：只要条件成立，就反复执行循环体。
    while (true) {
        // 【原第0791行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第0792行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "管理员菜单 > 菜品管理\n";
        // 【原第0793行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第0794行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "1. 查看全部菜品\n";
        // 【原第0795行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "2. 增加菜品\n";
        // 【原第0796行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "3. 删除菜品\n";
        // 【原第0797行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "4. 修改菜品\n";
        // 【原第0798行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "0. 返回上一级\n";

        // 【原第0800行】读取菜品管理菜单选择，允许 0 到 4；这是唯一功能性修复，保证第 4 项“修改菜品”能被选择。
        int choice = readInt("请选择：", 0, 4);
        // 【原第0801行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (choice == 0) return;

        // 【原第0803行】switch 多分支选择，根据 choice 的值进入不同 case。
        switch (choice) {
            // 【原第0804行】case 表示 switch 中的一个菜单选项分支。
            case 1:
                // 【原第0805行】调用显示全部菜品的函数。
                showAllDishes(dishes);
                // 【原第0806行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第0807行】case 表示 switch 中的一个菜单选项分支。
            case 2:
                // 【原第0808行】调用管理员相关全局函数，处理系统整体层面的管理功能。
                adminAddDish(dishes);
                // 【原第0809行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第0810行】case 表示 switch 中的一个菜单选项分支。
            case 3:
                // 【原第0811行】调用管理员相关全局函数，处理系统整体层面的管理功能。
                adminDeleteDish(dishes);
                // 【原第0812行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第0813行】case 表示 switch 中的一个菜单选项分支。
            case 4:
                // 【原第0814行】调用管理员相关全局函数，处理系统整体层面的管理功能。
                adminModifyDish(dishes);
                // 【原第0815行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
        // 【原第0816行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0817行】调用暂停函数，等待用户按回车后再继续显示菜单。
        pauseScreen();
    // 【原第0818行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0819行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0821行】下面进入一个新的功能区域，分隔线只用于提高代码可读性。
// ========================= 管理员全局函数：订单查询 =========================

// 【原第0823行】定义管理员查看全部订单的全局函数，需要遍历学生链表和每个学生的订单。
void adminShowAllOrders(const StudentList& students) 
// 【原第0824行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
    // 【原第0825行】赋值语句，把右边的结果保存到左边的变量或成员中。
    Student* current = students.getHead();
    // 【原第0826行】声明一个变量或对象，用来保存后续计算或管理的数据。
    bool hasOrder = false;

    // 【原第0828行】调用打印分隔线函数，让控制台界面更清楚。
    printLine('-');
    // 【原第0829行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << left
         // 【原第0830行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(12) << "学生"
         // 【原第0831行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(12) << "学号"
         // 【原第0832行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(10) << "订单号"
         // 【原第0833行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(14) << "菜品"
         // 【原第0834行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(8) << "数量"
         // 【原第0835行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(10) << "金额"
         // 【原第0836行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
         << setw(10) << "状态" << '\n';
    // 【原第0837行】调用打印分隔线函数，让控制台界面更清楚。
    printLine('-');

    // 【原第0839行】while 循环：只要条件成立，就反复执行循环体。
    while (current != nullptr) 
	// 【原第0840行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第0841行】定义常量，程序运行过程中这个值不会被修改。
        const vector<Order>& orders = current->getOrders();
        // 【原第0842行】for 循环：按指定规则遍历数据，这里常用于遍历数组、vector 或链表。
        for (const Order& order : orders) 
		// 【原第0843行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
		{
            // 【原第0844行】赋值语句，把右边的结果保存到左边的变量或成员中。
            hasOrder = true;
            // 【原第0845行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
            cout << left
                 // 【原第0846行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(12) << current->getName()
                 // 【原第0847行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(12) << current->getId()
                 // 【原第0848行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(10) << order.orderId
                 // 【原第0849行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(14) << order.dishName
                 // 【原第0850行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(8) << order.quantity
                 // 【原第0851行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(10) << fixed << setprecision(2) << order.amount
                 // 【原第0852行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                 << setw(10) << orderStatusToString(order.status) << '\n';
        // 【原第0853行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0854行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
        current = current->next;
    // 【原第0855行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0857行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (!hasOrder)
	// 【原第0858行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第0859行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "当前还没有任何订单。\n";
    // 【原第0860行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
    // 【原第0861行】调用打印分隔线函数，让控制台界面更清楚。
    printLine('-');
// 【原第0862行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0864行】定义按菜品名称关键字查询订单的全局函数。
void adminSearchOrdersByDish(const StudentList& students) 
// 【原第0865行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
    // 【原第0866行】声明一个变量或对象，用来保存后续计算或管理的数据。
    string keyword = readText("请输入要查询的菜品名称关键字：");
    // 【原第0867行】赋值语句，把右边的结果保存到左边的变量或成员中。
    Student* current = students.getHead();
    // 【原第0868行】声明一个变量或对象，用来保存后续计算或管理的数据。
    bool found = false;

    // 【原第0870行】调用打印分隔线函数，让控制台界面更清楚。
    printLine('-');
    // 【原第0871行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "订购包含【" << keyword << "】的菜品的学生：\n";
    // 【原第0872行】调用打印分隔线函数，让控制台界面更清楚。
    printLine('-');

    // 【原第0874行】while 循环：只要条件成立，就反复执行循环体。
    while (current != nullptr) {
        // 【原第0875行】定义常量，程序运行过程中这个值不会被修改。
        const vector<Order>& orders = current->getOrders();
        // 【原第0876行】for 循环：按指定规则遍历数据，这里常用于遍历数组、vector 或链表。
        for (const Order& order : orders) {
            // 【原第0877行】if 条件判断：括号里的条件成立才执行后面的代码块。
            if (order.dishName.find(keyword) != string::npos) 
			// 【原第0878行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
			{
                // 【原第0879行】赋值语句，把右边的结果保存到左边的变量或成员中。
                found = true;
                // 【原第0880行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                cout << "学生：" << current->getName()
                     // 【原第0881行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                     << "，学号：" << current->getId()
                     // 【原第0882行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                     << "，订单号：" << order.orderId
                     // 【原第0883行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                     << "，菜品：" << order.dishName
                     // 【原第0884行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                     << "，数量：" << order.quantity
                     // 【原第0885行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
                     << "，状态：" << orderStatusToString(order.status) << '\n';
            // 【原第0886行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
            }
        // 【原第0887行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0888行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
        current = current->next;
    // 【原第0889行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0891行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (!found) {
        // 【原第0892行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "没有查询到相关订单。\n";
    // 【原第0893行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0894行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0896行】定义按学生学号查询订单的全局函数。
void adminSearchOrdersByStudent(const StudentList& students) {
    // 【原第0897行】声明一个变量或对象，用来保存后续计算或管理的数据。
    string id = readText("请输入学生学号：");
    // 【原第0898行】赋值语句，把右边的结果保存到左边的变量或成员中。
    Student* student = students.findById(id);
    // 【原第0899行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (student == nullptr) {
        // 【原第0900行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "没有找到该学生。\n";
        // 【原第0901行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
        return;
    // 【原第0902行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0904行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "学生【" << student->getName() << "】的订单信息：\n";
    // 【原第0905行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
    student->showOrders();
// 【原第0906行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0908行】定义管理员订单查询子菜单函数。
void orderQueryMenu(const StudentList& students) {
    // 【原第0909行】while 循环：只要条件成立，就反复执行循环体。
    while (true) {
        // 【原第0910行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第0911行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "管理员菜单 > 订单查询\n";
        // 【原第0912行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第0913行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "1. 查看全部订单\n";
        // 【原第0914行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "2. 按学生查询订单\n";
        // 【原第0915行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "3. 按菜品名称查询订单\n";
        // 【原第0916行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "0. 返回上一级\n";

        // 【原第0918行】声明一个变量或对象，用来保存后续计算或管理的数据。
        int choice = readInt("请选择：", 0, 3);
        // 【原第0919行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (choice == 0) return;

        // 【原第0921行】switch 多分支选择，根据 choice 的值进入不同 case。
        switch (choice) {
            // 【原第0922行】case 表示 switch 中的一个菜单选项分支。
            case 1:
                // 【原第0923行】调用管理员相关全局函数，处理系统整体层面的管理功能。
                adminShowAllOrders(students);
                // 【原第0924行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第0925行】case 表示 switch 中的一个菜单选项分支。
            case 2:
                // 【原第0926行】调用管理员相关全局函数，处理系统整体层面的管理功能。
                adminSearchOrdersByStudent(students);
                // 【原第0927行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第0928行】case 表示 switch 中的一个菜单选项分支。
            case 3:
                // 【原第0929行】调用管理员相关全局函数，处理系统整体层面的管理功能。
                adminSearchOrdersByDish(students);
                // 【原第0930行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
        // 【原第0931行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第0932行】调用暂停函数，等待用户按回车后再继续显示菜单。
        pauseScreen();
    // 【原第0933行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0934行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0936行】下面进入一个新的功能区域，分隔线只用于提高代码可读性。
// ========================= 登录与主菜单 =========================

// 【原第0938行】定义管理员登录函数，校验管理员账号和密码。
bool adminLogin() {
    // 【原第0939行】name 保存姓名，用于界面显示和查询结果显示。
    string name = readText("管理员账号：");
    // 【原第0940行】password 保存该学生登录密码。
    string password = readText("管理员密码：");

    // 【原第0942行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (name == ADMIN_NAME && password == ADMIN_PASSWORD) {
        // 【原第0943行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "管理员登录成功。\n";
        // 【原第0944行】return 把结果返回给调用者，并结束当前函数。
        return true;
    // 【原第0945行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
    // 【原第0946行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "账号或密码错误。\n";
    // 【原第0947行】return 把结果返回给调用者，并结束当前函数。
    return false;
// 【原第0948行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0950行】定义学生登录函数，返回登录成功的学生对象指针。
Student* studentLogin(StudentList& students) {
    // 【原第0951行】声明一个变量或对象，用来保存后续计算或管理的数据。
    string id = readText("请输入学号：");
    // 【原第0952行】password 保存该学生登录密码。
    string password = readText("请输入密码：");

    // 【原第0954行】赋值语句，把右边的结果保存到左边的变量或成员中。
    Student* student = students.findById(id);
    // 【原第0955行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (student == nullptr || !student->checkPassword(password)) {
        // 【原第0956行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "学号或密码错误。\n";
        // 【原第0957行】return 把结果返回给调用者，并结束当前函数。
        return nullptr;
    // 【原第0958行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }

    // 【原第0960行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "学生登录成功，欢迎你，" << student->getName() << "！\n";
    // 【原第0961行】return 把结果返回给调用者，并结束当前函数。
    return student;
// 【原第0962行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0964行】定义学生注册函数，本质是创建新学生并插入链表。
void registerStudent(StudentList& students) {
    // 【原第0965行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
    cout << "\n【学生注册】\n";
    // 【原第0966行】赋值语句，把右边的结果保存到左边的变量或成员中。
    Student* newStudent = createStudentFromInput();
    // 【原第0967行】if 条件判断：括号里的条件成立才执行后面的代码块。
    if (students.addStudent(newStudent)) {
        // 【原第0968行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "注册成功，请返回主菜单登录。\n";
    // 【原第0969行】这一行打开一个新的代码块，后面的语句都属于这个作用范围。
    } else {
        // 【原第0970行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "注册失败：该学号已经存在。\n";
        // 【原第0971行】delete 释放 new 创建的对象，防止内存泄漏。
        delete newStudent;
    // 【原第0972行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第0973行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第0975行】定义管理员主菜单函数，连接学生管理、菜品管理、订单查询。
void adminMenu(StudentList& students, vector<Dish>& dishes) {
    // 【原第0976行】while 循环：只要条件成立，就反复执行循环体。
    while (true) {
        // 【原第0977行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第0978行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "校园食堂信息管理系统 > 管理员菜单\n";
        // 【原第0979行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第0980行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "1. 学生数据管理\n";
        // 【原第0981行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "2. 菜品管理\n";
        // 【原第0982行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "3. 订单查询\n";
        // 【原第0983行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "0. 退出管理员菜单\n";

        // 【原第0985行】声明一个变量或对象，用来保存后续计算或管理的数据。
        int choice = readInt("请选择：", 0, 3);
        // 【原第0986行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (choice == 0) return;

        // 【原第0988行】switch 多分支选择，根据 choice 的值进入不同 case。
        switch (choice) {
            // 【原第0989行】case 表示 switch 中的一个菜单选项分支。
            case 1:
                // 【原第0990行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
                studentManageMenu(students);
                // 【原第0991行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第0992行】case 表示 switch 中的一个菜单选项分支。
            case 2:
                // 【原第0993行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
                dishManageMenu(dishes);
                // 【原第0994行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第0995行】case 表示 switch 中的一个菜单选项分支。
            case 3:
                // 【原第0996行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
                orderQueryMenu(students);
                // 【原第0997行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
        // 【原第0998行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
    // 【原第0999行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第1000行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第1002行】定义学生主菜单函数，通过 student->成员函数 调用学生自己的功能。
void studentMenu(Student* student, vector<Dish>& dishes, int& nextOrderId) {
    // 【原第1003行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
    // 注意：这里 student 是指针，下面通过 student->成员函数 调用。
    // 【原第1004行】这是普通中文注释，解释接下来代码的作用，不会被编译器执行。
    // 这能直接体现题目要求中的“通过指针访问成员函数”和面向对象思想。
    // 【原第1005行】while 循环：只要条件成立，就反复执行循环体。
    while (true) {
        // 【原第1006行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第1007行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "校园食堂信息管理系统 > 学生菜单\n";
        // 【原第1008行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第1009行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "当前用户：" << student->getName() << "（" << student->getId() << "）\n";
        // 【原第1010行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "1. 查看菜品\n";
        // 【原第1011行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "2. 在线订餐\n";
        // 【原第1012行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "3. 取餐验证\n";
        // 【原第1013行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "4. 查询我的订单\n";
        // 【原第1014行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "5. 查看个人信息\n";
        // 【原第1015行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "0. 退出学生菜单\n";

        // 【原第1017行】声明一个变量或对象，用来保存后续计算或管理的数据。
        int choice = readInt("请选择：", 0, 5);
        // 【原第1018行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (choice == 0) return;

        // 【原第1020行】switch 多分支选择，根据 choice 的值进入不同 case。
        switch (choice) {
            // 【原第1021行】case 表示 switch 中的一个菜单选项分支。
            case 1:
                // 【原第1022行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
                student->viewDishes(dishes);
                // 【原第1023行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第1024行】case 表示 switch 中的一个菜单选项分支。
            case 2:
                // 【原第1025行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
                student->placeOrder(dishes, nextOrderId);
                // 【原第1026行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第1027行】case 表示 switch 中的一个菜单选项分支。
            case 3:
                // 【原第1028行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
                student->pickupOrder();
                // 【原第1029行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第1030行】case 表示 switch 中的一个菜单选项分支。
            case 4:
                // 【原第1031行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
                student->showOrders();
                // 【原第1032行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第1033行】case 表示 switch 中的一个菜单选项分支。
            case 5:
                // 【原第1034行】箭头运算符 -> 用于通过指针访问对象的成员函数或成员变量。
                student->showProfile();
                // 【原第1035行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
        // 【原第1036行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
        // 【原第1037行】调用暂停函数，等待用户按回车后再继续显示菜单。
        pauseScreen();
    // 【原第1038行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第1039行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第1041行】定义系统主菜单函数，负责管理员登录、学生登录、学生注册和退出。
void mainMenu(StudentList& students, vector<Dish>& dishes, int& nextOrderId) 
// 【原第1042行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{
    // 【原第1043行】while 循环：只要条件成立，就反复执行循环体。
    while (true) 
	// 【原第1044行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
	{
        // 【原第1045行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第1046行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "校园学生食堂信息管理系统\n";
        // 【原第1047行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第1048行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "1. 管理员登录\n";
        // 【原第1049行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "2. 学生登录\n";
        // 【原第1050行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "3. 学生注册\n";
        // 【原第1051行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "0. 退出系统\n";
        // 【原第1052行】调用打印分隔线函数，让控制台界面更清楚。
        printLine();
        // 【原第1053行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "测试管理员：admin / 123456\n";
        // 【原第1054行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
        cout << "测试学生：20260001 / 111111\n";

        // 【原第1056行】声明一个变量或对象，用来保存后续计算或管理的数据。
        int choice = readInt("请选择：", 0, 3);
        // 【原第1057行】if 条件判断：括号里的条件成立才执行后面的代码块。
        if (choice == 0) {
            // 【原第1058行】向控制台输出文字或变量；多个 << 可以连续拼接输出内容。
            cout << "感谢使用校园食堂信息管理系统，再见！\n";
            // 【原第1059行】return; 用在 void 函数中，表示提前结束当前函数，不返回具体数值。
            return;
        // 【原第1060行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }

        // 【原第1062行】switch 多分支选择，根据 choice 的值进入不同 case。
        switch (choice) {
            // 【原第1063行】case 表示 switch 中的一个菜单选项分支。
            case 1:
                // 【原第1064行】if 条件判断：括号里的条件成立才执行后面的代码块。
                if (adminLogin()) {
                    // 【原第1065行】调用管理员相关全局函数，处理系统整体层面的管理功能。
                    adminMenu(students, dishes);
                // 【原第1066行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
                }
                // 【原第1067行】调用暂停函数，等待用户按回车后再继续显示菜单。
                pauseScreen();
                // 【原第1068行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第1069行】这一行打开一个新的代码块，后面的语句都属于这个作用范围。
            case 2: {
                // 【原第1070行】赋值语句，把右边的结果保存到左边的变量或成员中。
                Student* student = studentLogin(students);
                // 【原第1071行】if 条件判断：括号里的条件成立才执行后面的代码块。
                if (student != nullptr) {
                    // 【原第1072行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
                    studentMenu(student, dishes, nextOrderId);
                // 【原第1073行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
                }
                // 【原第1074行】调用暂停函数，等待用户按回车后再继续显示菜单。
                pauseScreen();
                // 【原第1075行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
            // 【原第1076行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
            }
            // 【原第1077行】case 表示 switch 中的一个菜单选项分支。
            case 3:
                // 【原第1078行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
                registerStudent(students);
                // 【原第1079行】调用暂停函数，等待用户按回车后再继续显示菜单。
                pauseScreen();
                // 【原第1080行】break 结束当前 switch 分支或循环，防止继续执行后面的分支。
                break;
        // 【原第1081行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
        }
    // 【原第1082行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
    }
// 【原第1083行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

// 【原第1085行】下面进入一个新的功能区域，分隔线只用于提高代码可读性。
// ========================= 主函数 =========================

// 【原第1087行】定义程序入口函数，C++ 程序从这里开始执行。
int main()
// 【原第1088行】花括号表示一个代码块或类型定义的范围；分号结束类型定义。
{


    // 【原第1091行】声明一个变量或对象，用来保存后续计算或管理的数据。
    StudentList students;
    // 【原第1092行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
    vector<Dish> dishes;
    // 【原第1093行】next 是链表指针，指向下一个学生结点。
    int nextOrderId = 1;
    // 【原第1094行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
    initStudents(students);
    // 【原第1095行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
    initDishes(dishes);
    // 【原第1096行】这一行是 C++ 语句，配合上下文完成当前函数的具体操作。
    mainMenu(students, dishes, nextOrderId);
    // 【原第1097行】return 把结果返回给调用者，并结束当前函数。
    return 0;
// 【原第1098行】结束当前代码块，例如 if、while、for、函数或类中的某一层范围。
}

